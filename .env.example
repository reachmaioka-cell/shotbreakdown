NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
ANTHROPIC_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
CRON_SECRET=
OPENAI_API_KEY=
NEXT_PUBLIC_SITE_URL=
DATABASE_URL=

# In-app Pro upgrade. All three are required. Without them /upgrade shows a
# contact link rather than a payment path that cannot grant entitlement.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# Optional: error monitoring. Without this, failures still go to stdout.
SENTRY_DSN=

# Optional: Google search for clip research (https://serper.dev). Without this, research uses
# YouTube metadata + Wikipedia only.
SERPER_API_KEY=
